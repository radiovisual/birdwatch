'use strict';

// expose a constructor

// return an instance if it called without "new"

// add a feed with options

// add a feed without options

// error if no feed is supplied

// error if no screenname is supplied

// get twitter data

// removes filtered tweets

// removes retweets when remove_retweets = true

// fails if filter_tags isn't valid regex

// cache file saves

// tweets are sorted

